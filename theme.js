import theme from "mdx-deck/themes";

export default {
  // extends the default theme
  ...theme,
  // add a custom font
  font: "Roboto, sans-serif",
  // custom colors
  colors: {
    text: "#232f3e",
    background: "#ff9900",
    link: "#007eb9"
  }
};
